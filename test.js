/*
var ansTime = 10;
var counter;
    function run() {
      counter = setInterval(decrement, 1000);
    }
        function decrement() {
        	ansTime--;
        	console.log(ansTime)
        	$("#timer").html("<h2>" + ansTime + "</h2>");
        	if (ansTime === 0) {
        		stop();
        	}
        }
        function stop() {
      		clearInterval(counter);
    }
run()

var interval = setInterval(function() {
    console.log('i');
}, 1000);
console.log(interval);*/